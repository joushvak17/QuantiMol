[project]
name = "quantimol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
]

[dependency-groups]
dev = [
    "chembl-webresource-client>=0.10.9",
    "ipykernel>=6.29.5",
]
